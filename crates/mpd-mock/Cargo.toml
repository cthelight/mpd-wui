[package]
name = "mpd-mock"
description = "In-process mock MPD server for tests (dev-only, never shipped)."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
tokio = { workspace = true }
